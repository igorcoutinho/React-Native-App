import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    alignItems: 'center',
    height: 35,
    justifyContent: 'center',
    width: 35,
    position: 'absolute',
    top: 10,
    left: 10,
    zIndex: 100,
  },
  rightAccessory: {
    alignItems: 'center',
    height: 35,
    justifyContent: 'center',
    width: 35,
    position: 'absolute',
    top: 10,
    right: 10,
    zIndex: 100,
  },
});
