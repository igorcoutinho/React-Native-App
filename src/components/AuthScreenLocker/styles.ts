import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: 'rgba(33, 36, 87, 0.9)',
    flex: 1,
    height: '100%',
    justifyContent: 'center',
    width: '100%',
    paddingHorizontal: 20,
  },
});
